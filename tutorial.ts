// Add
// this.afs.collection('/COLLECTION').add({});

// Get
//this.afs.collection('/COLLECTION').get().pipe(map((snapshot) => <Hero[]>snapshot.docs.map(doc => doc.data())))

// Update
// this.afs.collection('users').doc(this.userService.getUser()?.uid).set({
//   ...this.userService.getUser(),
//   displayName: displayName
// })
